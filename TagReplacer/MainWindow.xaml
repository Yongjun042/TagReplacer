<Window x:Class="TagReplacer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TagReplacer"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            
        </Grid.ColumnDefinitions>

        <Button Name="btnOpenFile" Click="BtnOpenFile_Click" Grid.Row="0">Open file</Button>
        <ListView Name="myListView" Grid.Row="1" ItemsSource="{Binding MusicTagList}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="StringData" Width="150" DisplayMemberBinding="{Binding Tag.Title}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
